language: node_js
node_js:
    - "6"
    - "10"
    - "11"
dist: trusty # needs Ubuntu Trusty
sudo: required
addons:
  chrome: stable # have Travis install chrome stable.
cache:
  directories:
    - node_modules
install:
  - npm ci
script:
  - npm run test
